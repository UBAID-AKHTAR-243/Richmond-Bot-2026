        /* Chat Header - Corrected */
        .chat-header {
            padding: 0 15px; /* Sets horizontal padding, vertical is handled by flex */
            background: var(--whatsapp-panel-bg);
            display: flex;
            align-items: center; /* Vertically centers the contact info and icons */
            justify-content: space-between;
            height: 60px; /* Fixed height */
            border-bottom: 1px solid var(--whatsapp-border);
        }
